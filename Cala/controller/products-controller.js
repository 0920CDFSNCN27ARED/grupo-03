const path = require("path");

const controller = {
  //products: (req, res) => {
  //  res.render("products"));
  //},

  details: function (req, res) {
    res.render("details");
  },
};

module.exports = controller;
