<!DOCTYPE html>
<html lang="en">
  <head>
    <%-include("../partials/head-base", { title: user.user}) %>
    <script src="/js/confirm-delete-product.js"></script>
  </head>

  <body class="background-main">
    <%-include("../partials/header") %>
    <main class="flex-wrap">
      <section
        class="seccion-details-conteiner flex-column background-main width-100"
      >
        <div class="d-flex-lg width-100vw">
          <div class="width-50lg">
            <div>
              <h1 class="font-arimo color-primary margin-10 center-text">
                <%= user.firstName %> <%= user.lastName %>
              </h1>
            </div>
            <article
              class="product-article-details flex-column no-border radius-5"
            >
              <img
                src="/images/users/<%= user.image %>"
                alt="<%= user.user %>"
                class="width-77"
              />
            </article>
          </div>

          <article
            class="flex-column text-details-container font-arimo width-50-lg profile-lg"
          >
            <div class="flex-column">
              <h1 class="font-arimo color-primary margin-10 center-text">
                <%= user.email %>
              </h1>
              <h1 class="font-arimo color-primary margin-10 center-text">
                <%= user.user %>
              </h1>
            </div>
            <div class="d-flex">
              <div class="details-form d-flex width-100 no-border-lg">
                <a href="/<%= user.id  %>/edit " class="width-100"
                  ><button type="submit" class="radius-5 border-1">
                    Editar
                  </button></a
                >
              </div>

              <div class="details-form d-flex width-100 no-border-lg">
                <form id="deleteForm" class="width-100">
                  <button
                    id="deleteButton"
                    type="submit"
                    class="radius-5 border-1"
                  >
                    Borrar
                  </button>
                </form>
                <div id="confirmDelete" class="hidden">
                  <p class="font-arimo color-primary">
                    Estas seguro que quieres dejar de ser usuario?
                  </p>
                  <form
                    id="reconfirm"
                    action="/<%= user.id  %>?_method=DELETE"
                    method="POST"
                  >
                    <button
                      type="submit"
                      id="deleteYes"
                      class="radius-5 border-1"
                    >
                      Si
                    </button>
                  </form>

                  <form action="/profile/<%= user.id  %>" method="GET">
                    <button
                      type="submit"
                      id="deleteNo"
                      class="radius-5 border-1"
                    >
                      No
                    </button>
                  </form>
                </div>
              </div>
            </div>
          </article>
        </div>
      </section>
    </main>
    <%-include("../partials/footer") %>
  </body>
</html>
